# AutoFFmpeg

Skeleton project structure for the AutoFFmpeg refactor.

The canonical behavior is currently implemented in `golden_anchor/pubg_hevc_gui.py`.
New modules under `autoffmpeg/` will gradually absorb logic from the golden anchor
while preserving functional parity.
